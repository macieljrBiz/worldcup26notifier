<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Animation Storyboards -->

    <!-- Feed Item Entrance Animation (Slide + Fade) -->
    <Storyboard x:Key="FeedItemEntranceAnimation">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="0" To="1" 
                         Duration="0:0:0.3" />
        <ThicknessAnimation Storyboard.TargetProperty="(FrameworkElement.Margin)" 
                            From="20,0,0,0" To="0,0,0,0" 
                            Duration="0:0:0.3" />
    </Storyboard>

    <!-- Status Color Transition -->
    <Storyboard x:Key="StatusColorTransition">
        <ColorAnimation Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)" 
                        Duration="0:0:0.2" />
    </Storyboard>

    <!-- Pulse Animation (for LIVE badges) -->
    <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="1" To="0.6" 
                         Duration="0:0:0.5" 
                         AutoReverse="True" />
    </Storyboard>

    <!-- Progress Bar Live Animation (for LIVE matches) -->
    <Storyboard x:Key="ProgressBarLiveAnimation" RepeatBehavior="Forever">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="1" To="0.7" 
                         Duration="0:0:0.8" 
                         AutoReverse="True" />
    </Storyboard>

    <!-- Button Hover Scale -->
    <Storyboard x:Key="ButtonHoverScale">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                         To="1.05" 
                         Duration="0:0:0.15" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                         To="1.05" 
                         Duration="0:0:0.15" />
    </Storyboard>

    <!-- Button Hover Restore -->
    <Storyboard x:Key="ButtonHoverRestore">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                         To="1" 
                         Duration="0:0:0.15" />
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                         To="1" 
                         Duration="0:0:0.15" />
    </Storyboard>

    <!-- Dialog Fade In -->
    <Storyboard x:Key="DialogFadeIn">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="0" To="1" 
                         Duration="0:0:0.15" />
    </Storyboard>

    <!-- Dialog Fade Out -->
    <Storyboard x:Key="DialogFadeOut">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="1" To="0" 
                         Duration="0:0:0.1" />
    </Storyboard>

    <!-- View Fade Out (for transitioning away from current view) -->
    <Storyboard x:Key="ViewFadeOut">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="1" To="0" 
                         Duration="0:0:0.15" />
    </Storyboard>

    <!-- View Fade In (for transitioning to new view) -->
    <Storyboard x:Key="ViewFadeIn">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="0" To="1" 
                         Duration="0:0:0.2" />
    </Storyboard>

    <!-- View Slide In From Right (new view enters from right) -->
    <Storyboard x:Key="ViewSlideInFromRight">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="0" To="1" 
                         Duration="0:0:0.25" />
        <ThicknessAnimation Storyboard.TargetProperty="(FrameworkElement.Margin)" 
                            From="50,0,0,0" To="0,0,0,0" 
                            Duration="0:0:0.25" />
    </Storyboard>

    <!-- View Slide Out To Left (current view exits to left) -->
    <Storyboard x:Key="ViewSlideOutToLeft">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" 
                         From="1" To="0" 
                         Duration="0:0:0.2" />
        <ThicknessAnimation Storyboard.TargetProperty="(FrameworkElement.Margin)" 
                            From="0,0,0,0" To="-50,0,0,0" 
                            Duration="0:0:0.2" />
    </Storyboard>

</ResourceDictionary>
